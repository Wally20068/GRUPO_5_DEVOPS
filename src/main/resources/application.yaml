spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://grupo-5-rds.cylow2yyoc7q.us-east-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    deserialization:
      fail-on-unknown-properties: false

logging:
  pattern:
    level: "%5p [%X{correlationId}]"